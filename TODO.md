# FinTree — Актуальный TODO

_Обновлено: 2026-02-19_

## Как читать этот файл

- `P0` — критично, влияет на корректность данных и базовые сценарии пользователя.
- `P1` — важно, влияет на предсказуемость и качество продуктового UX.
- `P2` — улучшения UX/UI, не блокирующие базовую корректность.
- Для задач в этом файле новые автотесты не планируются; проверка выполняется вручную.

---

## P0 — Критичные задачи



---

## P1 — Важные продуктовые задачи



- [ ] `FT-TODO-009` Пересобрать иконки категорий с учетом контекста доходов/расходов.  
  **Критерий приемки:** используются 2 curated-набора иконок (для доходов и для расходов) с фолбэком для старых значений.

- [x] `FT-TODO-011` **(Accounts)** Рефакторинг фильтров и панели инструментов.
  **Критерий приемки:** панель инструментов на странице счетов упрощена, удалены лишние элементы.
  - Убрать блок сортировки (`.accounts-sort`).
  - Убрать кнопку сброса фильтров.
  - Убрать текстовую подсказку "Корректировка баланса доступна...".
  - Поисковая строка должна стать более компактной.
  - **Компонент:** `vue-app/src/pages/AccountsPage.vue`.

- [ ] `FT-TODO-012` **(Accounts)** Приоритетный показ основного счета.
  **Критерий приемки:** основной счет пользователя всегда отображается первым в сетке счетов, независимо от других фильтров.
  - **Компонент:** `vue-app/src/pages/AccountsPage.vue` (логика в `filteredAccounts`).

- [ ] `FT-TODO-013` **(Accounts)** Рефакторинг карточки счета (`AccountCard`).
  **Критерий приемки:** дизайн карточки счета обновлен для компактности и удобства.
  - Действия "Корректировка баланса" и "Сделать основным" перенесены в контекстное меню (три точки).
  - Убран контейнер `.card-balance`.
  - Сумма в оригинальной валюте отображается меньшим шрифтом.
  - Удалены текстовые метки "Валюта" и "Ликвидность".
  - **Компонент:** `vue-app/src/components/AccountCard.vue`.

- [ ] `FT-TODO-014` **(Accounts)** Упрощение модального окна создания/редактирования счета.
  **Критерий приемки:** форма стала проще и более сфокусированной.
  - В режиме создания убрано поле "Тип счета".
  - В режиме редактирования убраны поля "Тип счета" и "Валюта". Заголовок изменен на "Переименование счета".
  - **Компонент:** `vue-app/src/components/AccountFormModal.vue`.

- [ ] `FT-TODO-015` **(Accounts)** Рефакторинг модального окна корректировки баланса.
  **Критерий приемки:** окно корректировки стало проще и понятнее для пользователя.
  - Заголовок окна теперь включает имя счета (напр., "Изменение баланса для 'ТБанк'").
  - Убрана история корректировок и дублирующая информация о счете.
  - Поле "Фактический баланс" переименовано в "Текущий баланс".
  - Добавлен информер с подсказкой к полю "Текущий баланс".
  - **Компонент:** `vue-app/src/components/AccountBalanceAdjustmentsModal.vue`.

- [ ] `FT-TODO-021` **(Transactions)** Объединить создание транзакции и перевода.
  **Критерий приемки:** единое модальное окно используется для добавления расходов, доходов и переводов, упрощая UX.
  - Кнопка "Перевод" удалена из хедера страницы.
  - `TransferFormModal` удален и его функциональность встроена в `TransactionForm` (например, через табы).
  - **Компоненты:** `ExpensesPage.vue`, `TransactionForm.vue`.

- [ ] `FT-TODO-023` **(Design System)** Заменить цветовую палитру и семантические токены.
  **Критерий приемки:** Вся старая цветовая система в `design-tokens.css` заменена на новую премиум-палитру (Onyx, Jet Black, Light Olive). Все семантические токены (`--ft-bg-base`, `--ft-text-primary` и т.д.) переключены на новую палитру. Несовместимая светлая тема (`.light-mode`) удалена.

- [x] `FT-TODO-024` **(Design System)** Стандартизировать и уменьшить радиусы скругления.
  **Критерий приемки:** В `design-tokens.css` внедрена новая, более строгая и консистентная шкала радиусов (`--ft-radius-sm: 4px` и т.д.) для достижения "менее округлого" премиум-стиля.

- [x] `FT-TODO-025` **(Design System)** Аудит и исправление жестко заданных радиусов (border-radius).
  **Критерий приемки:** Все жестко заданные значения `border-radius` в коде (например, `8px`, `50%`) заменены на соответствующие новые дизайн-токены (`var(--ft-radius-lg)`, `var(--ft-radius-full)`). Поиск по коду не находит больше таких значений.

- [ ] `FT-TODO-026` **(Design System)** Аудит и исправление жестко заданных цветов.
  **Критерий приемки:** Все жестко заданные цвета (`#...`, `rgb`...) в файлах `.vue` и `.css` заменены на семантические дизайн-токены. Единственным местом определения цветов должен остаться `design-tokens.css`.

---

## P2 — Улучшения UX/UI

- [ ] `FT-TODO-010` Сильно сократить и перекомпоновать лендинг.  
  **Критерий приемки:** лендинг построен по компактной структуре «hero + ключевые секции» без перегруза текстом.

- [ ] `FT-TODO-016` **(Analytics)** Упростить карточки, удалив подзаголовки.
  **Критерий приемки:** подзаголовки (`.card-subtitle`) удалены из ключевых карточек на странице аналитики для создания более чистого вида.
  - Затронутые компоненты: `SpendingPieCard`, `SpendingBarsCard`, `CategoryDeltaCard`, `ForecastCard`.

- [ ] `FT-TODO-017` **(Analytics)** Редизайн карточки прогноза расходов (`ForecastCard`).
  **Критерий приемки:** карточка прогноза стала более информативной и легкой для восприятия.
  - Удален чип статуса (`.forecast-chip`).
  - Внедрено семантическое кодирование цветом: "риск" - предупреждающий, "оптимистичный" - позитивный, основной "прогноз" - акцентный.
  - Карточки с суммами прогнозов переработаны для лучшей читаемости и компактности.

- [ ] `FT-TODO-018` **(Investments)** Упростить UI страницы инвестиций.
  **Критерий приемки:** со страницы убраны второстепенные текстовые элементы для более чистого вида.
  - Удален подзаголовок (`.card-subtitle`) из карточки "Распределение по счетам".
  - Удалена подсказка (`.accounts-hint`) из секции со списком счетов.
  - **Компоненты:** `InvestmentsAllocationPie.vue`, `InvestmentsAccountsSection.vue`.

- [ ] `FT-TODO-019` **(Investments)** Редизайн карточки инвестиционного счета.
  **Критерий приемки:** карточка счета стала более компактной, а действия сгруппированы в меню.
  - Удалена дата последнего обновления и значок валюты.
  - Переключатель "ликвидный/неликвидный" перемещен к названию счета.
  - Удален футер карточки.
  - Добавлено контекстное меню (три точки) с действием "Переименовать счет".
  - **Компонент:** `InvestmentsAccountsSection.vue` (или его дочерний компонент-карточка).

- [ ] `FT-TODO-020` **(Transactions)** Упростить хедер на странице транзакций.
  **Критерий приемки:** в хедере страницы остались только основные действия.
  - Удалены кнопки "Экспорт" и "Категории" (и из десктопной версии, и из мобильного меню).
  - Кнопка "Добавить транзакцию" переименована в "Добавить".
  - **Компонент:** `vue-app/src/pages/ExpensesPage.vue`.

- [ ] `FT-TODO-022` **(Transactions)** Визуально упростить фильтры транзакций.
  **Критерий приемки:** панель фильтров в списке транзакций стала более компактной и менее навязчивой.
  - **Компонент:** `TransactionList.vue`.