# 📊 Техническое задание: Страница аналитики (FinTree)

## 🧩 Технологии и принципы

- **Фреймворк:** Vue 3  
- **UI-библиотека:** PrimeVue + PrimeIcons  
- **Графики:** PrimeVue Chart (на базе Chart.js)  
- **Тема:** поддержка светлой и тёмной, цвета — мягкие, неяркие, с опорой на токены PrimeVue  
- **Данные:** использовать **только реальные DTO с бэкенда**, без моков и вычислений на фронте  
- **Состояния:** loading / empty / error должны быть реализованы через `Skeleton` и `Message`

---

## 🧭 Общая структура страницы

Страница состоит из 5 карточек, расположенных в сетке:
- Desktop ≥ 1280px → 2 колонки  
- Mobile → 1 колонка  

```vue
<template>
  <div class="grid gap-4 xl:grid-cols-2">
    <HeroHealthCard class="xl:col-span-2" />
    <SpendingPieCard />
    <NetWorthLineCard />
    <SpendingBarsCard />
    <ForecastCard />
  </div>
</template>
```

---

## 1️⃣ Hero-блок «Финансовое здоровье»

**Цель:** показать пользователю общую оценку финансового состояния и состояние ключевых метрик.

### Компоненты PrimeVue:
`Card`, `Tag` / `Badge`, `ProgressBar`, `OverlayPanel`, `Button` (иконка `pi pi-info-circle`)

### Содержимое:
- Крупный числовой **индекс (0–100)**  
- Вердикт: “Хорошо”, “Средне”, “Плохо” (цвет зависит от значения)
- Таблица метрик:
  - Savings Rate
  - Liquidity Months
  - Expense Volatility
  - Income Diversity
- Цветовые индикаторы:
  - 🟢 `--green-400` — хорошо  
  - 🟡 `--yellow-400` — средне  
  - 🔴 `--red-400` — плохо  
- Кнопка `i` открывает пояснение к каждой метрике (`OverlayPanel`)

### Состояния:
- `loading`: скелетон на индекс и метрики  
- `error`: сообщение с иконкой `pi pi-exclamation-triangle`  
- `empty`: текст “Нет данных”

---

## 2️⃣ Круговая диаграмма расходов по категориям

**Цель:** визуализировать распределение расходов за выбранный период.

### Компоненты PrimeVue:
`Card`, `SelectButton`, `Chart (type='pie')`, `Tag`

### Содержимое:
- Заголовок: “Расходы по категориям”
- Переключатель периода: **1 / 3 / 6 / 12 месяцев**
- Диаграмма (`PieChart`)
- Легенда:
  - Цвет категории (из бэка)
  - Название
  - Сумма и процент

### Цвета:
- Использовать `category.color`, если приходит с бэка  
- Если нет — использовать последовательность PrimeVue (`--blue-400`, `--teal-400`, `--violet-400`, `--cyan-400`, `--orange-400`)

### Состояния:
- `loading`: круглый скелетон + пустая легенда  
- `empty`: “Нет данных за период”, иконка `pi pi-inbox`

---

## 3️⃣ Линейная диаграмма нетворса (Net Worth)

**Цель:** показать динамику изменения капитала по месяцам.

### Компоненты:
`Card`, `Dropdown` (кол-во месяцев: 6/12/24), `Chart (type='line')`

### Содержимое:
- Заголовок: “Изменение капитала”
- Ось X — месяцы (MMM YYYY)
- Ось Y — сумма в базовой валюте (из бэка)
- Tooltip: “Январь 2025 — 1 200 000 ₽”
- Пунктирная линия — среднее значение (если приходит с бэка)

### Цвета:
- Основная линия: `--primary-400`
- Средняя: `--surface-500`

### Состояния:
- `loading`: скелетон
- `empty`: “Нет данных”
- `error`: сообщение об ошибке

---

## 4️⃣ Столбчатая диаграмма расходов по периодам

**Цель:** показать интенсивность расходов по дням / неделям / месяцам.

### Компоненты:
`Card`, `SelectButton`, `Chart (type='bar')`, `Calendar` (опционально)

### Содержимое:
- Заголовок: “Динамика расходов”
- Переключатель детализации: **День / Неделя / Месяц**
- Диаграмма:  
  - Ось X — даты/недели/месяцы  
  - Ось Y — сумма расходов  
  - Tooltip: интервал и сумма

### Цвета:
- Столбики: `--orange-400` (мягкий), при ховере насыщеннее  
- Сетка: `--surface-500`

### Состояния:
- `loading`: скелетон  
- `empty`: сообщение “Нет данных за период”

---

## 5️⃣ Прогноз расходов до конца месяца

**Цель:** показать прогноз трат при текущем уровне расходов.

### Компоненты:
`Card`, `ProgressBar`, `Tag`

### Содержимое:
- Заголовок: “Прогноз расходов”
- Основной текст:  
  “Если вы сохраните текущий уровень трат, к концу месяца вы потратите ≈ **X ₽**.”
- Подтекст: “Сейчас: A ₽ из B ₽”
- Прогресс-бар:
  - Значение: `A / B * 100`
  - Цвет:
    - 🟢 прогноз ниже лимита
    - 🟡 близко к лимиту
    - 🔴 превышен лимит

### Состояния:
- `loading`: скелетон  
- `empty`: “Недостаточно данных для прогноза”

---

## 🎨 Цветовая система

- Основные токены PrimeVue:
  - Текст: `--text-color`, `--text-color-secondary`
  - Поверхности: `--surface-0 ... --surface-900`
  - Акцент: `--primary-400`, `--primary-500`
- Сигнальные цвета:
  - `--green-400` (успех)
  - `--yellow-400` (предупреждение)
  - `--red-400` (ошибка)
- Прозрачности через `rgba(var(--primary-500-rgb), .15)` для фонов диаграмм
